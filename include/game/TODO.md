# TODO - game

- [ ] Add `ScreenID` enum (`MainMenu`, `Options`, `CharacterSelect`, `Match`, `Replay`, `etc`).
- [ ] Add `Action` enum (`UI_Up`/ `Down`/ `Confirm`/ `Back`, etc.)
- [ ] Decide on input representation for rollback (`bitset` or `struct`)
- [ ] Add configuration values for:
  - logical resolution (e.g. 1920x1080)
  - scaling policy (letterbox)
  - debug flags (draw hitboxes, show frame index, console window, etc)

