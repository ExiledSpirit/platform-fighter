# TODO - input

- [ ] Implement `ActionsEvent` (pressed/released/repeat)
- [ ] Implement `ActionMap` (binginds per context/ screen)
  - UI context
  - Match context (later...)
- [ ] Implement `InputRouter`
  - consume raylib key states
  - emit ActionEvents into EventBus (`core::EventBus`)
- [ ] Add support for gamepads
- [ ] Add input buffering helpers (useful later for fighting mechanics)

